apiVersion: v1
kind: Service
metadata:
  name: {{ include "moons-keydb.fullname" . }}
  labels:
    {{- include "moons-keydb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moons-keydb.selectorLabels" . | nindent 4 }}
