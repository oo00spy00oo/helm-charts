apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moons-keydb.fullname" . }}-test-connection"
  labels:
    {{- include "moons-keydb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moons-keydb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
